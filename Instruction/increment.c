#include<stdio.h>
int main ()
{
    int i=5;
    printf("the value after i++ is %d\n",i++ );
    i/=10;
    printf(" the value of is %d\n",i);
    // ++i phle increment fir print 
    // i++ phle print fir increment
    //  same with decrement operator --i or i-- 

    return 0;
}
